version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused
    - exhaustive
    - bodyclose
    - sqlclosecheck
    - rowserrcheck
    - noctx
  settings:
    exhaustive:
      default-signifies-exhaustive: true
    staticcheck:
      checks:
        - all
        - -QF1001
        - -QF1002
        - -QF1012
        - -S1009
        - -S1016
        - -ST1000
        - -ST1003
        - -SA1019
  exclusions:
    # Vendored third-party Go files inside web/node_modules (e.g. the
    # `flatted` package ships a Go reference impl) trip govet/staticcheck
    # whenever we lint at the repo root. They're not part of our build.
    paths:
      - web/node_modules/.*
    rules:
      - path: internal/db/gen/
        linters:
          - govet
          - ineffassign
          - staticcheck
          - unused
          - exhaustive
          - bodyclose
          - sqlclosecheck
          - rowserrcheck
          - noctx
      - path: _test\.go
        linters:
          - bodyclose
          - noctx
          - staticcheck
      - path: test/
        linters:
          - bodyclose
          - noctx

formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/onscreen/onscreen
