<script lang="ts">
  // Placeholder landing; replaced in step 4 with real hub.
</script>

<div class="landing">
  <h1>OnScreen</h1>
  <p>webOS client scaffold — step 1 of 7.</p>
</div>

<style>
  .landing {
    padding: var(--page-pad);
  }

  h1 {
    font-size: var(--font-2xl);
    margin: 0 0 16px;
    color: var(--accent);
  }

  p {
    font-size: var(--font-md);
    color: var(--text-secondary);
    margin: 0;
  }
</style>
