{
  "name": "onscreen-roku",
  "version": "0.1.0",
  "private": true,
  "description": "OnScreen Roku channel — sideload + package scripts",
  "type": "module",
  "scripts": {
    "package": "node scripts/package.mjs",
    "sideload": "node scripts/sideload.mjs",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
