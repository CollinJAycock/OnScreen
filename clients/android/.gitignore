*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
# Release signing artefacts — never commit. local.properties points
# at the keystore; both stay out of git.
*.keystore
*.jks
