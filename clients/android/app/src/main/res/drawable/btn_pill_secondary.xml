<?xml version="1.0" encoding="utf-8"?>
<!--
    Pill-shaped secondary action — sits next to btn_pill_primary on the
    detail page. Translucent white surface with a soft border so the
    button is visible against any fanart but doesn't compete with the
    primary CTA. Focus state matches btn_pill_primary's accent flip.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="#33ffffff" />
            <stroke android:width="2dp" android:color="@color/accent" />
            <corners android:radius="24dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#22ffffff" />
            <stroke android:width="1dp" android:color="#44ffffff" />
            <corners android:radius="24dp" />
        </shape>
    </item>
</selector>
