package tv.onscreen.mobile.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette mirrors the web client's dark-mode tokens so the
// phone app feels like the same product. Not derived from a Material
// You seed because the web identity is the canonical surface.
val OnScreenAccent = Color(0xFF7C5CFF)
val OnScreenAccentDark = Color(0xFF5B3FE0)
val OnScreenSurfaceDark = Color(0xFF121212)
val OnScreenSurfaceVariantDark = Color(0xFF1E1E22)
val OnScreenOnSurfaceDark = Color(0xFFEDEDED)
val OnScreenOnSurfaceMutedDark = Color(0xFFA0A0A0)
