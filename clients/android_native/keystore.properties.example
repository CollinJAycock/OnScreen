# Copy to ../keystore.properties (one directory up — module root)
# and fill in. The real file is gitignored.
storeFile=clients/android_native/keystore/upload.jks
storePassword=REPLACE_ME
keyAlias=upload
keyPassword=REPLACE_ME
