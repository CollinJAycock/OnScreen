## Summary

<!-- What does this change do, and why? 1–3 sentences. Link any related issue. -->

## Changes

<!-- Bullet list of user-visible or architectural changes. Skip if trivial. -->

-

## Testing

<!-- What did you run to verify this? -->

- [ ] `make test-unit` passes
- [ ] `make lint` passes
- [ ] Frontend changes: `cd web && npx svelte-check` passes
- [ ] Schema changes: new goose migration added under `internal/db/migrations/`
- [ ] SQL changes: `make generate` rerun and `internal/db/gen/` committed

## Notes for reviewer

<!-- Anything the reviewer should focus on, known caveats, follow-ups, or deferred work. Delete if none. -->
